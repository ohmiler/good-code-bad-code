export const metadata = {
  title: "Dataclasses for data shapes",
  track: "python",
  order: 7,
  summary: "Use dataclasses for stable internal data shapes instead of loose dictionaries with repeated string keys.",
  tags: ["dataclasses", "data", "models"],
  takeaways: ["A named data shape makes fields, defaults, and equality behavior easier to review."],
  goodCode: {
    language: "python",
    filename: "review_summary.py",
    code: `from dataclasses import dataclass


@dataclass(frozen=True)
class ReviewSummary:
    review_id: int
    title: str
    approved: bool


def summarize_review(review):
    # The named shape makes every returned field explicit.
    return ReviewSummary(
        review_id=review.id,
        title=review.title,
        approved=review.status == "approved",
    )`,
  },
  badCode: {
    language: "python",
    filename: "review_summary.py",
    code: `def summarize_review(review):
    # Vague dict keys make the returned shape easy to misunderstand.
    return {
        "id": review.id,
        "name": review.title,
        "ok": review.status == "approved",
    }`,
  },
};

## What to review

The good version gives the shape a name and makes every field explicit.

The bad version returns a loose dictionary with vague keys. Callers have to remember what `"name"` and `"ok"` mean, and typos will be found late.

## Review notes

Dictionaries are useful at boundaries like JSON. Inside the application, a small dataclass often makes the contract easier to review and test.
