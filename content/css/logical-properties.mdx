export const metadata = {
  title: "Logical properties",
  track: "css",
  order: 8,
  summary: "Use flow-relative spacing and borders when styles should adapt to writing direction.",
  tags: ["internationalization", "layout", "maintainability"],
  takeaways: ["Prefer inline and block logical properties when left/right/top/bottom are not the real intent."],
  goodCode: {
    language: "css",
    filename: "article-callout.css",
    code: `.article-callout {
  margin-block: 1.5rem;
  padding-block: 1rem;
  padding-inline: 1.25rem;
  border-inline-start: 4px solid #2563eb;
}

.article-callout__meta {
  text-align: start;
}`,
  },
  badCode: {
    language: "css",
    filename: "article-callout.css",
    code: `.article-callout {
  margin-top: 24px;
  margin-bottom: 24px;
  padding: 16px 20px 16px 20px;
  border-left: 4px solid #2563eb;
}

.article-callout__meta {
  text-align: left;
}`,
  },
};

## What to review

The good version says the accent belongs at the start edge and the spacing belongs to the block or inline axis, so the component can adapt to different writing directions.

The bad version hard-codes left and right even when the intent is start, end, inline, or block. That makes localization and layout reuse more expensive later.

## Review notes

In review, ask whether a physical direction is truly required. If the design means "start of the text" or "space above and below", logical properties usually describe the intent more directly.
