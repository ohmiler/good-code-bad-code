export const metadata = {
  title: ".gitignore and secret safety",
  track: "git",
  order: 9,
  summary: "Keep generated files and secrets out of version control, and remove tracked secrets from the index immediately.",
  tags: ["gitignore", "secrets", "security"],
  takeaways: ["A repository should make accidental secret and build artifact commits hard to create and easy to catch."],
  goodCode: {
    language: "bash",
    filename: "secret-safety.sh",
    code: `# Prevent local secrets and generated output from being staged.
printf ".env\\n.env.local\\ndist/\\nnode_modules/\\n" >> .gitignore
git rm --cached .env
git status --short
git add .gitignore
git commit -m "ignore local secrets and build output"`,
  },
  badCode: {
    language: "bash",
    filename: "secret-safety.sh",
    code: `# Adding everything can publish secrets or local machine state.
git add .
git commit -m "add config"
git push`,
  },
};

## What to review

The good version ignores local secret files and removes an already-tracked secret from the index before committing.

The bad version stages every file, which can include credentials, generated artifacts, editor files, or local environment settings.

## Review notes

`.gitignore` is prevention, not cleanup after the fact. If a secret was committed, removing it from a later commit is not enough; the team may need key rotation and history cleanup.
