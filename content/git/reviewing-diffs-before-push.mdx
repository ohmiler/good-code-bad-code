export const metadata = {
  title: "Reviewing diffs before push",
  track: "git",
  order: 6,
  summary: "Inspect unstaged, staged, and committed changes before pushing so accidental files and debug edits do not leave your machine.",
  tags: ["diff", "push", "review"],
  takeaways: ["A push should come after a local review pass, not be the first time anyone sees the final diff."],
  goodCode: {
    language: "bash",
    filename: "pre-push-review.sh",
    code: `# Check both file content and the commits about to leave the branch.
git diff --check
git diff
git diff --staged
git log --oneline origin/main..HEAD
git push`,
  },
  badCode: {
    language: "bash",
    filename: "pre-push-review.sh",
    code: `# The first real review happens after the push, which is too late.
git add .
git commit -m "final"
git push`,
  },
};

## What to review

The good version checks whitespace issues, reviews both unstaged and staged changes, and confirms which commits are about to be pushed.

The bad version sends the final state upstream without a local review pass.

## Review notes

Before pushing, reviewers should be able to ask "what exactly leaves this branch?" The answer should come from `diff` and `log`, not memory.
