export const metadata = {
  title: "Configuration and secrets",
  track: "php",
  order: 9,
  summary: "Load configuration from the environment and fail clearly when required secrets are missing.",
  tags: ["configuration", "secrets", "environment"],
  takeaways: ["Secrets should be runtime configuration, not strings committed next to application logic."],
  goodCode: {
    language: "php",
    filename: "config/database.php",
    code: `<?php declare(strict_types=1);

$databaseUrl = getenv('DATABASE_URL');

if ($databaseUrl === false || $databaseUrl === '') {
    throw new RuntimeException('DATABASE_URL is required.');
}

return [
    'url' => $databaseUrl,
    'timeout_seconds' => 3,
];`,
  },
  badCode: {
    language: "php",
    filename: "config/database.php",
    code: `<?php

return [
    'url' => 'mysql://root:secret-password@localhost/app',
    'timeout_seconds' => $_GET['timeout'] ?? 30,
];`,
  },
};

## What to review

The good version reads a required secret from the runtime environment and fails during startup if it is missing.

The bad version commits credentials and allows a request value to influence infrastructure configuration.

## Review notes

Configuration review should answer two questions quickly: where does this value come from, and can user input change it?
