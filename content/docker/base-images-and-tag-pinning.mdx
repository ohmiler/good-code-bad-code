export const metadata = {
  title: "Base images and tag pinning",
  track: "docker",
  order: 2,
  summary: "Choose a narrow base image and pin versions deliberately so rebuilds do not change underneath you without review.",
  tags: ["base image", "tags", "supply chain"],
  takeaways: ["A base image is a dependency; review it with the same care as package versions."],
  goodCode: {
    language: "dockerfile",
    filename: "Dockerfile",
    code: `FROM node:22.11.0-alpine3.20 AS runtime

WORKDIR /app
ENV NODE_ENV=production

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY server.js ./server.js
CMD ["node", "server.js"]`,
  },
  badCode: {
    language: "dockerfile",
    filename: "Dockerfile",
    code: `FROM node:latest

WORKDIR /app
COPY . .
RUN npm install

CMD ["node", "server.js"]`,
  },
};

## What to review

The good version uses a specific runtime base and keeps production dependency installation explicit.

The bad version depends on `latest`, so rebuilding the same commit can silently pull a different Node version and operating system packages.

## Review notes

Pinning does not mean never updating. It means updates become visible changes that CI, security scanners, and reviewers can evaluate.
