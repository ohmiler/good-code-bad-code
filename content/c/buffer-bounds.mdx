export const metadata = {
  title: "Buffer bounds and formatted output",
  track: "c",
  order: 2,
  summary: "Use bounded formatting APIs and check their return values before treating a fixed buffer as complete.",
  tags: ["buffers", "bounds", "formatting"],
  takeaways: ["A bounded write still needs a return-value check, because truncation can be a data bug rather than a safe result."],
  goodCode: {
    language: "c",
    filename: "src/review_label.c",
    code: `#include <stddef.h>
#include <stdio.h>

int format_review_label(char *dst, size_t dst_size, int id)
{
    // snprintf reports when the label does not fit.
    int written = snprintf(dst, dst_size, "review-%d", id);
    if (written < 0 || (size_t) written >= dst_size) {
        return -1;
    }

    return 0;
}`,
  },
  badCode: {
    language: "c",
    filename: "review_label.c",
    code: `#include <stdio.h>

void format_review_label(char *dst, int id)
{
    // sprintf cannot see the destination size.
    sprintf(dst, "review-%d", id);
}`,
  },
};

## What to review

The good version uses `snprintf`, passes the buffer size, and treats truncation as a failure. The caller receives a result code instead of a silently shortened label.

The bad version uses `sprintf`, so the function cannot prove the destination is large enough. A longer id can overwrite adjacent memory.

## Review notes

For fixed buffers, review both the API and the return value. A bounded function prevents many overwrites, but the code still has to decide whether truncated data is allowed.
